package appUI;


public class calcControl {
  // 计算两个单词间的最短路径
  public String calcShortestPath(digraph G, String word1, String word2) {
    String res = G.calcShortestPath(G, word1, word2);
    return res;
  }
}
